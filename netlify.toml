# Static site — no build step. Push to GitHub deploys when the repo is linked in Netlify.
[build]
  publish = "."

# Netlify serves index.html at / by default; map root to this page.
[[redirects]]
  from = "/"
  to = "/aggie-lee.html"
  status = 200

# Clean URL for the support thank-you page.
[[redirects]]
  from = "/thank-you"
  to = "/thank-you.html"
  status = 200
